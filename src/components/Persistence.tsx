export default function Persistence(): JSX.Element {
  // const rule = new schedule.RecurrenceRule();
  // rule.hour = 17;

  // const job = schedule.scheduleJob("Finding a word for today and posting it to db", '* 1 * * *', a
  // );

  // setInterval(() => {
  //   const date = new Date();
  //   alert(date.getMinutes());
  //   if ((date.getMinutes() % 5) == 0) {
  //     setCurrentWord(getRandomWord(words))}}, 60000);

  // const axiosRequest1 = axios.post(`${API_BASE}/todayword`, {
  //   word: currentWord,
  // });
  // const axiosRequest2 = axios.get(`${API_BASE}/today`);

  // useEffect(() => {
  //   const fn = async () =>
  //     await axiosRequest1
  //     .then(async function(){
  //       const today = await axiosRequest2
  //       .then(function(response) {
  //         setTodayWord(response.data[0].word)
  //       }
  //       )
  //     })
  //     .catch(function (error) {
  //       console.log(error);
  //     })
  // }, [currentWord]);

  return <></>;
}
